---
- name: update_tmux_plugin_perms
  file:
    path: /home/{{username}}/.tmux/plugins
    owner: "{{ username }}"
    group: "{{ username}}"
    recurse: true
    
- name: restart os
  ansible.builtin.reboot:
  listen: restart os