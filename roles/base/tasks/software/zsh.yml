---
- name: install zsh
  pacman:
    name:
      - zsh
      - z
      - fzf
      - zsh-autosuggestions
      - zsh-syntax-highlighting
    state: present

- name: change "{{username}}"'s shell to zsh
  become: yes
  user:
    name: "{{ username }}"
    shell: /usr/bin/zsh


